import React from 'react';

const Header = () => {

    return (
        <div className="bg-[#1d2125]"> {/* we use brackets because of custom colors or else we could use bg-yellow-500 directly */}
            header
        </div>
    );
}
export default Header;